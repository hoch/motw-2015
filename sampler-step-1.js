/**
 * Step 1
 * - loadAudioFile from sound directory
 * - bufferSource -> filter -> amp ADSR enveloping
 *
 * Step 2
 * - multiple buffer sources (samplePlayer: filter + ADSR)
 * - drum sampler + keyboards
 *
 * Step 3
 * - notification
 */